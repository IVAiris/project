# Plan-runtime.md — Фаза «Основа runtime»

## 0. Контекст

Тип проекта: учебный (24-часовой MVP «ЮрСправочник»). Уровень плана: **micro** — покрывает одну фазу из `project_plan_24h.md` («3:00–4:30 Основа runtime»), не весь проект.

**Предыдущий слой:** спецификация (`spec.md`) и база знаний (`README.md`, `knowledge/services.md`, `faq.md`, `rules.md`) — уже написаны, выверены на соответствие `urok.md` и закоммичены в git.

**Следующий слой:** «Безопасные tools» (4:30–6:00, `project_plan_24h.md`) — реализация `agent/tools.py` (`search_knowledge`, `read_knowledge_file`, `prepare_lead_draft`, `save_confirmed_lead`). Этот план сознательно не реализует сами tools — только точку их вызова (`call_tool`) как заглушку.

## 1. Статус

Приоритет: высокий — блокирует все последующие AI-сценарии. Текущее состояние: не начато, `.env` с реальным ключом уже создан и проверен (`test_connection.py` успешно вызвал модель). Секреты не печатаются в открытом виде — только ссылка на `.env`/`.env.example` (см. `sensitive-data`). Источник контекста: `spec.md` (разделы 5–7), `urok.md` (Фаза 3), `agent-py.md`, `ai_studio_api_notes.md`.

## 1.1 Целевой сценарий

Разработчик (не конечный пользователь бота, а тот, кто ведёт эту фазу) запускает `agent_runtime.py` локально с тестовым сообщением → runtime вызывает модель через `ai_client.py` → если модель вызывает инструмент, runtime логирует шаг, вызывает заглушку `call_tool` (пока без реальных tools) → runtime возвращает финальный ответ модели или контролируемое сообщение об остановке по `MAX_STEPS` → всё это видно в логе шагов.

## 2. Требования к результату

**Что должно получиться:** `ai_client.py`, `agent/soul.md`, `agent_runtime.py` в `BOT/project/`; лимит шагов и логирование; проверенный биллинг AI Studio.

**Явные ограничения (что не делаем в этом плане):**
- Не реализуем сами функции tools (`search_knowledge` и т.д.) — это следующий слой.
- Не подключаем FastAPI/HTTP-роуты — это ещё позже (Web UI, 10:00–13:00).
- Не трогаем `knowledge/`, `README.md`, `spec.md`.

**Файлы, разрешённые к изменению:** `BOT/project/ai_client.py` (новый), `BOT/project/agent/soul.md` (новый), `BOT/project/agent/__init__.py` (новый, пустой, чтобы `agent` был пакетом), `BOT/project/agent_runtime.py` (новый). Существующие файлы не редактируются.

**Снимок контракта данных (env vars, из `.env.example`, уже зафиксировано — не переопределяется здесь):**
`YANDEX_API_KEY`, `YANDEX_FOLDER_ID`, `YANDEX_MODEL_URI` (полный `gpt://{folder}/{model}`), `YANDEX_BASE_URL`, `YANDEX_TEMPERATURE` (float, дефолт `0.4`), `YANDEX_MAX_TOKENS` (int, дефолт `10000`).

**Снимок контракта вызова модели (из `ai_studio_api_notes.md`, не переопределяется — только используется):**
- Метод: `client.chat.completions.create(model=..., messages=[...], tools=[...], tool_choice="auto")`.
- Возвращаемое значение — **полное сообщение** (`response.choices[0].message`), а не только `.content`, чтобы runtime видел `.tool_calls`.
- `message.tool_calls[i].function.name` / `.function.arguments` (JSON-строка) — контракт для парсинга вызова инструмента.
- Результат инструмента возвращается модели как `{"role": "tool", "tool_call_id": tool_call.id, "content": <JSON-строка результата>}`.

**Снимок контракта сессии (из `spec.md`, раздел 5, не переопределяется):**
`sessions[session_id] = {"messages": [...], "mode": "bot_flow" | "ai_consultant", "flow_state": ..., "lead_draft": None}` — этот план создаёт структуру данных и загрузку истории в память, но не сам HTTP-слой, который её создаёт (это Web UI фаза).

**`MAX_STEPS = 6`** — зафиксировано дословно в `agent-py.md:924`, не открытый вопрос.

## 3. Шаги

- [x] **Действие:** Пользователь вручную открывает консоль AI Studio и проверяет баланс/лимиты после уже выполненного тестового запроса (`test_connection.py`).
      **Результат (ожидаемый):** Подтверждено, что запрос списался с ожидаемого баланса и лимиты не близки к исчерпанию.
      **Факт выполнения:** Подтверждено скриншотом «первый запрос через API.png» — на нём одновременно виден успешный ответ модели (`test_connection.py`) и баланс/лимиты в консоли AI Studio. Оба факта закрыты одним артефактом.
      **Проверка:** Пользователь подтвердил содержимое скриншота текстом (баланс/лимиты видны, ключ на скриншоте не публикуется в чат).
      **Ошибки:** нет.

- [x] **Действие:** Создать `agent/soul.md` — системная инструкция консультанта: роль (ЮрСправочник, консультация по трём услугам), разрешённые tools (только 4 из `agent/tools.py`, ещё не реализованных — перечислить по имени), правило «не выдумывать данные вне `knowledge/`», правило честного отказа, формат черновика заявки, запрет читать `.env`/файлы вне `knowledge/`.
      **Результат (ожидаемый):** Файл `agent/soul.md` с системным промптом, согласованным с ограничениями из `knowledge/rules.md` («Границы консультанта»).
      **Факт выполнения:** Создан `BOT/project/agent/soul.md`. Содержит роль, источник ответов (только 3 файла `knowledge/`), все 4 tool по именам, жёсткие правила (не выдумывать, не читать `.env`/файлы вне `knowledge/`, не выполнять shell, не более 1–2 уточнений, `save_confirmed_lead` только после подтверждения, эскалация по критериям `rules.md`), формат черновика заявки, формат ответа.
      **Проверка:** Текст инструкции не противоречит `knowledge/rules.md` и `spec.md` разделу 5 (ИИ-консультант); явно перечислены все 4 tool по именам. Сверено вручную построчно — противоречий не найдено.
      **Ошибки:** нет.

- [x] **Вход:** переменные окружения из `.env` (не читаются напрямую этим шагом, только через `python-dotenv` в коде).
      **Действие:** Создать `ai_client.py` — `load_dotenv(PROJECT_ROOT / ".env")` с `PROJECT_ROOT = Path(__file__).resolve().parent` (дословно по `agent-py.md:804-811`, а не относительный `load_dotenv()` без пути — иначе поиск `.env` сломается при запуске из другого рабочего каталога, например через `uvicorn` в фазе Web UI); функция вызова модели через `openai.OpenAI(api_key=..., base_url=..., project=YANDEX_FOLDER_ID)`, метод `chat.completions.create(model=YANDEX_MODEL_URI, messages=..., tools=..., tool_choice="auto", temperature=YANDEX_TEMPERATURE, max_tokens=YANDEX_MAX_TOKENS)`, обёрнутый в `try/except` (см. `spec.md`, раздел 6 — сбой API не роняет процесс, логируется только класс ошибки).
      **Результат (ожидаемый):** Функция возвращает полный объект `message` (с `.content` и `.tool_calls`) либо контролируемо поднимает/возвращает признак ошибки без секретов в логе.
      **Факт выполнения:** Создан `ai_client.py`. `call_model()` возвращает `response.choices[0].message` целиком (не только `.content`). Ошибки любого типа оборачиваются в `AIStudioError(type(exc).__name__)` — в исключение попадает только имя класса ошибки, не текст/секреты. Ручной прогон: `call_model([{"role": "user", "content": "Ответь одним словом: тест"}])` → `msg.content == "тест"`, `msg.tool_calls is None`, процесс не упал.
      **Проверка:** Выполнен ручной вызов через `python -c` — вывод подтверждает `.content` и `.tool_calls` доступны одновременно.
      **Ошибки:** нет.

- [x] **Действие:** Создать `agent_runtime.py` по структуре из `agent-py.md` (helpers `_get_messages`, `_tool_call_to_dict`, `_assistant_message_to_dict`, `_parse_arguments`, функция `run_agent(user_task, system_prompt, conversation_history=None)`); определить в этом же файле класс `ToolError(Exception)` — он будет переиспользован `agent/tools.py` в следующей фазе (импорт оттуда, а не наоборот). Цикл `for step in range(1, MAX_STEPS + 1)`: вызов `ai_client`, если `message.tool_calls` — парсинг JSON-аргументов через `_parse_arguments` (поднимает `ToolError` на невалидном JSON), вызов `call_tool(name, args)` (пока заглушка: whitelist из 4 имён, для известных — `NotImplementedError`, для неизвестных — `ToolError`), добавление `role: tool` в историю, иначе — возврат финального ответа; при достижении `MAX_STEPS` — контролируемое сообщение об остановке (не исключение). Дополнительно добавить блок `if __name__ == "__main__":` — временная точка входа для ручной проверки этой фазы (по аналогии с ролью `main.py` в `agent-py.md`, шаг 8 эталонного плана, но без построения полноценного терминального интерфейса): читает `agent/soul.md` как текст (путь через тот же `PROJECT_ROOT`, что и в `ai_client.py`), вызывает `run_agent(user_task="...", system_prompt=...)` с тестовым вопросом, печатает результат. Этот блок — временный инструмент разработки, не часть финального продукта; будет вытеснен реальным вызовом из FastAPI-роута в фазе Web UI.
      **Результат (ожидаемый):** Рабочий loop, который для сообщения без необходимости в tools отвечает с первого шага. Полноценная проверка ветки `tool_calls` невозможна до появления `TOOL_DEFINITIONS` в `agent/tools.py` (следующая фаза) — без схемы `tools=[...]`, переданной в `chat.completions.create()`, модель физически не вернёт `tool_calls`, поэтому в рамках этой фазы ветка dispatch остаётся написанной, но не протестированной живым вызовом.
      **Факт выполнения:** _(пусто)_
      **Проверка:** `python agent_runtime.py` → вывод `[task] Ответь одним словом: тест`, `[loop] step 1`, `[final] тест` — совпадает с ожидаемым форматом дословно. Тест реального `tool_calls`-раунда — в плане следующей фазы («Безопасные tools»), не здесь.
      **Ошибки:** нет.

- [x] **Действие:** Добавить логирование по точному формату тегов из `agent-py.md:990-1036` — `print(f"[task] {user_task}")` в начале `run_agent`, `print(f"[loop] step {step_number}")` на каждой итерации, `print(f"[tool] selected: {tool_name}")` и `print(f"[tool] arguments: ...")` при вызове tool, `print(f"[refusal] {exc}")` при `ToolError`/отказе — без секретов и без полного текста `problem_text`/контактов на этом этапе (реальные данные появятся только в следующих фазах).
      **Результат (ожидаемый):** В stdout видна пошаговая трассировка ровно с этими тегами (`[task]`, `[loop]`, `[tool]`, `[refusal]`), без утечки `YANDEX_API_KEY` и без излишних персональных данных.
      **Факт выполнения:** Реализовано внутри `run_agent()`/`call_tool()` в `agent_runtime.py`. Проверено тремя реальными прогонами (не декларативно): (1) `ai_client.py` — принудительная ошибка модели корректно обёрнута в `AIStudioError`, в сообщении только имя класса (`PermissionDeniedError`), `assert YANDEX_API_KEY not in msg` прошёл; (2) `call_tool`/`_parse_arguments` напрямую — известный tool → `NotImplementedError`, неизвестный → `ToolError`, валидный/невалидный/не-объектный JSON — все ветки сработали верно; (3) полный `run_agent()` с подменённым `call_model` — смоделирован `tool_calls`-раунд: `[tool] selected` → `[tool] arguments` → `[refusal]` → цикл продолжился на step 2 и получил финальный ответ (значит `role: tool` сообщение реально дошло до модели); отдельно проверено, что при бесконечных `tool_calls` цикл останавливается ровно на `MAX_STEPS = 6` с `[refusal]`-сообщением об остановке (`assert calls["n"] == MAX_STEPS` прошёл).
      **Проверка:** Все три прогона выполнены в текущей сессии, вывод построчно просмотрен — секретов, полного текста системной инструкции или лишних данных в логе нет.
      **Ошибки:** нет.

## 4. Прямые зависимости и проверки

- Источник истины по env-переменным: `.env.example` (не дублируется здесь).
- Источник истины по методу вызова модели: `ai_studio_api_notes.md`.
- Источник истины по паттерну loop/`MAX_STEPS`/`call_tool`: `agent-py.md` (терминальный урок).
- Источник истины по ограничениям консультанта: `knowledge/rules.md`.
- Порядок зависимостей: `agent/soul.md` не зависит от кода и может писаться параллельно с `ai_client.py`; `agent_runtime.py` зависит от готового `ai_client.py` (импортирует функцию вызова модели).
- **Направление импорта между фазами:** `ToolError` определяется в `agent_runtime.py` (эта фаза); `agent/tools.py` (следующая фаза) импортирует `ToolError` оттуда и определяет свой `TOOL_DEFINITIONS` — не наоборот. Несоблюдение этого направления в следующей фазе создаст кольцевой импорт.
- **Ограничение тестируемости этой фазы:** без `TOOL_DEFINITIONS` (следующая фаза) модель не может вернуть `tool_calls` — раздел кода `call_tool`/`ToolError` в этой фазе останется написанным, но не проверенным живым вызовом. Это не дефект этой фазы, а ожидаемое следствие порядка фаз в `project_plan_24h.md`.
- Типовая ошибка всей фазы: спутать `chat.completions.create` с `responses.create` (уже разобрано и закрыто в `ai_studio_api_notes.md` — не повторять).

## 5. Критерии готовности

- [x] `agent/soul.md` существует и не противоречит `knowledge/rules.md`.
- [x] `ai_client.py` возвращает полный объект `message`, обрабатывает сетевые ошибки без падения процесса — подтверждено принудительным вызовом ошибки (`AIStudioError`, без утечки ключа).
- [x] `agent_runtime.py` реализует loop с `MAX_STEPS = 6`, `ToolError`, логированием по тегам `[task]/[loop]/[tool]/[refusal]`, корректной обработкой `role: tool` — диспетчеризация и `MAX_STEPS`-остановка подтверждены симуляцией `tool_calls` через подмену `call_model` (реальный вызов модели с настоящей схемой tools — предмет следующей фазы).
- [x] `python agent_runtime.py` (self-test блок `if __name__ == "__main__":`) подтверждает прямой ответ модели на вопрос, не требующий tool. Проверка **живого** вызова модели, которая сама решает вызвать tool (а не симуляции) — критерий готовности **следующей** фазы, не этой (нет `TOOL_DEFINITIONS` до появления `agent/tools.py`).
- [x] Биллинг/лимиты AI Studio проверены пользователем вручную — подтверждено скриншотом «первый запрос через API.png».
- [x] В логах нет секретов и излишних персональных данных — проверено во всех прогонах этой фазы.
