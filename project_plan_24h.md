# План реализации MVP за 24 часа

Ниже — рабочий план на **24 часа** для проекта.
Его смысл: сначала перенести проверенный паттерн из терминального урока — `ai_client.py`, `agent_runtime.py`, tools, `soul.md`, `.env`, ограничения и логи — а затем добавить только обязательную веб-обвязку проекта.
Терминальный урок уже даёт корректную основу: модель выбирает tool, Python валидирует вызов, выполняет разрешённую функцию и возвращает результат модели; система не должна доверять prompt как механизму безопасности.

## Реалистичный объём

MVP:

> **ЮрСправочник — веб-чат с ИИ-консультантом по внутренней базе знаний юрдепартамента. Пользователь получает консультацию по типовым вопросам и шаблонам, может подготовить черновик по утверждённому шаблону или передать нетиповой вопрос юристу.**

Оставить ровно три услуги:
1. Консультация по типовым вопросам и шаблонам.
2. Подготовка черновика по утверждённому шаблону.
3. Передача вопроса юристу.

Это даёт необходимый каталог из 3–5 услуг, не требует работы с загрузкой файлов или document review и позволяет показать обычную и AI-подготовленную заявки.

## Зафиксированные технические решения

- **Backend:** FastAPI + uvicorn (единый ASGI-процесс — один Python runtime, встроенная pydantic-валидация для HTTP-слоя и для аргументов tools, неблокирующий I/O на время ожидания ответа AI Studio API).
- **FAQ-категория «стоимость»:** для внутренней легенды ЮрСправочника (сервис для сотрудников компании, не внешний платный сервис) категория переосмыслена как **SLA/сроки/приоритет обращения** — сохраняет формальное требование урока о четырёх FAQ-категориях без искусственных внешних цен.

## План на 24 часа

| Время | Результат | Что конкретно сделать |
|---|---|---|
| 0:00–1:30 | Спецификация | Создать `spec.md`, `Plan.md`, `AGENTS.md`; зафиксировать легенду, 3 услуги, границы MVP, критерии приёмки и запрещённые расширения. Подход `Spec → Plan → AGENTS.md → Work → Check → README`. |
| 1:30–3:00 | Knowledge base | Написать компактные `knowledge/services.md`, `knowledge/faq.md`, `knowledge/rules.md`; подготовить 6–10 карточек типовых материалов — шаблоны, регламенты, инструкции. |
| 3:00–4:30 | Основа runtime | Перенести паттерн AI Studio: `.env.example`, `ai_client.py`, `agent/soul.md`, `agent_runtime.py`, лимит шагов и логирование. В `ai_client.py` вернуть сообщение модели целиком, чтобы runtime видел `tool_calls`. После первого тестового запроса к модели проверить биллинг и лимиты AI Studio API. |
| 4:30–6:00 | Безопасные tools | Сделать `search_knowledge`, `read_knowledge_file`, `prepare_lead_draft`, `save_confirmed_lead`; добавить whitelist путей, только `.md` внутри `knowledge/`, запрет `.env`, абсолютных путей и выхода через `..`. Многоуровневая проверка путей и аргументов. |
| 6:00–7:30 | SQLite | Создать `data/bot.sqlite3`, `leads` и `feedback`; добавить `source = bot_flow` и `source = ai_consultant`; проверить сохранение записей вручную. |
| 7:30–10:00 | Обычный бот | Реализовать `bot_flow.py`: меню, услуги, FAQ, обычная заявка, feedback. Этот режим должен работать полностью без API-ключа и без LLM. |
| 10:00–13:00 | Web UI | Сделать один простой web-чат: сообщения, меню быстрых действий, форма/состояние заявки, кнопки подтверждения черновика. Не строить кабинет, страницы администрирования или сложную навигацию. |
| 13:00–15:30 | AI-сценарии | Связать UI с агентом: поиск знаний, чтение документа, краткий ответ с указанием материала, подбор шаблона, предложение обращения при отсутствии ответа. |
| 15:30–17:00 | Черновик и подтверждение | Реализовать строгое правило: `prepare_lead_draft` создаёт только черновик; `save_confirmed_lead` срабатывает только после явной кнопки «Отправить обращение». |
| 17:00–19:00 | Тестирование | Проверить bot-flow, AI-ответ по базе, AI-черновик, обычную заявку, feedback, отказы на `.env`, `../`, несуществующий файл и вопрос вне базы. Сохранить проверяемые логи шагов и отказов. |
| 19:00–20:30 | README | Описать установку, `.env`, команды запуска, структуру, сценарии проверки, ограничения ИИ, SQLite, безопасные tools и заметку о проверке биллинга и лимитов AI Studio. |
| 20:30–22:00 | VPS | Развернуть тот же репозиторий: `venv`, `pip install -r requirements.txt`, `.env` только на сервере, запуск веб-приложения, повторный запуск после остановки. |
| 22:00–24:00 | Доказательства | Сделать 5 скриншотов, провести финальный smoke test, проверить `.gitignore`, отсутствие ключей и персональных данных, обновить README фактическими командами. |

## Что перенести почти без изменений

Из терминального урока можно практически дословно использовать следующие архитектурные решения:

| Компонент | Что сохранить |
|---|---|
| `.env.example` | Переменные AI Studio без реальных значений. |
| `ai_client.py` | OpenAI-compatible вызов AI Studio и возврат полного assistant message с `tool_calls`. |
| `agent_runtime.py` | Цикл: model call → tool call → Python validation → tool result → model call → final answer. |
| `agent/soul.md` | Роль, допустимые tools, правило не выдумывать, правило отказа. |
| `MAX_STEPS` | Защита от бесконечного agent loop. |
| `call_tool()` | Whitelist tools, контроль обязательных и лишних аргументов, structured JSON-результаты. |
| Логи | Лог задачи, шага, выбранного tool, аргументов, результата или отказа. |
| Проверки границ | Запрет `.env`, абсолютных путей, `..`, неизвестных tools, неподходящих расширений. |

## Что писать с нуля

Это не было частью терминального урока и требует отдельного времени:

- web-чат и клиентская логика;
- HTTP API между браузером и backend;
- `bot_flow.py` без LLM;
- SQLite и схема `leads` / `feedback`;
- подтверждение AI-черновика через UI;
- запуск web-приложения на VPS;
- скриншоты по чек-листу проекта.

## Минимум content

Не тратить время на большую базу знаний.
Достаточно трёх небольших Markdown-файлов и около 1 500–2 500 слов суммарно:

```text
knowledge/
├── services.md   # 3 услуги, порядок работы, сроки, формат
├── faq.md        # 4 категории FAQ: услуги, SLA/сроки/приоритет, формат работы, правила/контакты
└── rules.md      # границы консультанта, регламент обращения,
                  # описания шаблонов и типовых кейсов
```

Для демонстрации подготовить 6–8 точных вопросов, на которые в этих файлах есть очевидные ответы: “где найти шаблон”, “какие данные нужны для черновика”, “когда направлять вопрос юристу”, “какой срок первичного ответа”. ИИ должен сначала обращаться к базе, а не отвечать общими знаниями.

## Самые опасные точки

Не начинать эти задачи до прохождения базовых сценариев:

- Загрузка и анализ пользовательского файла.
- Автоматическая проверка договора по чек-листу.
- Генерация полного юридического документа.
- Векторная БД, embeddings и внешний RAG-сервис.
- Авторизация, роли, личный кабинет.
- Интеграции с СЭД, 1С, e-mail, Telegram.
- Реальная отправка обращения в корпоративную систему.

Они не нужны для заданного agent pattern или основного проекта и критически увеличивают риск не успеть сделать обязательные сценарии, README, VPS и доказательства.

## Порог готовности

К моменту **17-го часа** должны быть уже готовы и вручную проверены:

1. Web-чат открывается локально.
2. `bot-flow` работает без `.env`.
3. FAQ и каталог доступны.
4. Обычная заявка сохраняется в SQLite.
5. Feedback сохраняется отдельно.
6. AI-консультант находит ответ только в `knowledge/`.
7. AI создаёт черновик, но не сохраняет его без кнопки подтверждения.

Если на этом этапе всё работает, оставшиеся часы — это деплой, README, скриншоты и исправление мелких проблем.
Если нет — нужно вырезать косметику, а не добавлять функции.

## Точка роста (архитектурный резерв, не MVP)

Подключение RAG и локальной LLM в текущий 24-часовой MVP не входит, но архитектура должна допускать это без переделки UI и `bot_flow.py`:

- `ai_client.py` — единая точка подключения источника модели, чтобы позже заменить AI Studio на локальную LLM без изменения `agent_runtime.py`.
- `search_knowledge` — отдельная функция/модуль, чтобы позже заменить файловый поиск на RAG.
- `knowledge/` — короткие структурированные Markdown-файлы, чтобы упростить будущий chunking и индексацию.
- `agent_runtime.py` — tool-логика не смешивается с HTTP/UI-слоем, чтобы retrieval-бэкенд можно было сменить отдельно.

В MVP реализуются только: AI Studio API как единственный источник ответов, `knowledge/` как локальная файловая база, `search_knowledge`/`read_knowledge_file` как файловые tools — без embeddings, vector DB, chunking-пайплайна, ingestion-сервиса и локального inference.

## Стратегия реализации

Самый безопасный порядок:

1. Сначала обычный чат + SQLite без AI.
2. Затем AI Studio + tools в терминале или через простой endpoint.
3. Потом подключить AI к web-интерфейсу.
4. В самом конце сделать deployment и доказательства.

Так останется работающий продукт даже при проблеме с API, а проектное требование базового `bot-flow` без LLM будет закрыто.
