# Plan-vps.md — VPS (16:00–17:30, `project_plan_24h.md`)

## 0. Контекст

Проект — учебный MVP («ЮрСправочник»). Уровень плана — micro-Plan на одну
фазу «16:00–17:30 VPS» (`project_plan_24h.md`), не на весь проект. Первая
фаза с таким профилем: ИИ-помощник не имеет SSH-доступа к реальному VPS —
предыдущих `Plan-*.md` с этим же ограничением в `docs/archive/` нет
(ближайший образец формата — `docs/archive/Plan-testing.md`, но там ИИ имел
прямой доступ к локальному `uvicorn`). Источник задач — `urok.md`, Фаза 6
(строки 146–160, 9 критериев) и «Формат сдачи» (5-й скриншот, строки 200,
219–220), `spec.md` раздел 7 (способ развёртывания — `nohup`/`disown`,
паттерн `server_summery.md`).

## 1. Статус

Приоритет — текущая активная фаза цикла `spec.md → Plan.md → CLAUDE.md →
Code → Check → README.md`; `spec.md` уже актуализирован (раздел 7 — способ
деплоя, раздел 12 — SSH выполняет пользователь). Секреты нигде не
печатаются в открытом виде — реальный `.env` (уже существует локально, 509
байт) не читается, не цитируется; на VPS создаётся пользователем
самостоятельно по `.env.example`. Источник контекста — `urok.md` Фаза 6,
`spec.md` разделы 7 и 12, `server_summery.md` (вне `project/`, паттерн
`nohup`/логи).

**Границы ответственности — явно, во избежание путаницы:** ИИ-помощник
готовит проверенный список команд, чек-лист проверки и обновляет
документацию по присланным пользователем фактам (вывод команд, логи без
секретов, подтверждение). ИИ-помощник не выполняет SSH-сессию сам и не
видит реальный VPS — каждый шаг с реальным выполнением на сервере помечен
явно, и «Факт выполнения» там заполняет пользователь, не ИИ.

## 1.1 Целевой сценарий

Пользователь на реальном VPS: клонирует репозиторий → создаёт `.venv` →
устанавливает зависимости → создаёт `.env` с реальными значениями (не
коммитит) → запускает приложение в фоне (`nohup`/`disown`) → проверяет, что
оно отвечает и создаёт `data/bot.sqlite3` → смотрит логи (без секретов) →
останавливает и запускает заново без ошибок → делает скриншот работающего
запуска.

## 2. Требования к результату

**Результат:** все 9 критериев Фазы 6 `urok.md` подтверждены пользователем
на реальном VPS; `evidence/screenshots/05-vps-runtime-start.png` подготовлен
и передан ИИ-помощнику для проверки на секреты перед фиксацией в плане;
раздел «16:00–17:30 VPS» `project_plan_24h.md` — `[x]` по каждому пункту.

**Явно не делаем в этом плане:** README (следующая фаза — инструкции
запуска/перезапуска пишутся ПОСЛЕ того, как они реально проверены здесь, не
раньше), финальный smoke test и push в `origin/main` (фаза «Доказательства»),
выбор/аренда самого VPS-провайдера (решено пользователем раньше), настройку
домена/публичной ссылки (прямо исключено `urok.md` для этой фазы), реальную
проверку работы ИИ-консультанта через AI Studio API с VPS (не входит в 9
критериев Фазы 6 — только запуск/логи/перезапуск; если понадобится отдельно,
это новый живой вызов API, требующий отдельного КАПС-ЛОК согласования по
`CLAUDE.md`, раздел 4).

**Файлы, разрешённые к изменению этим планом:**
- `project_plan_24h.md` — только статус чек-листа раздела «16:00–17:30 VPS»
  (после подтверждения пользователем каждого пункта).
- `requirements.txt` — только закрепление точных версий уже используемых
  зависимостей (Шаг 4), без добавления новых пакетов.
- `evidence/screenshots/05-vps-runtime-start.png` — новый файл, только после
  того, как пользователь его сделает и пришлёт на проверку.
- Никаких других файлов репозитория и никаких файлов на VPS ИИ-помощник не
  создаёт и не редактирует — у него нет туда доступа.

**Порт и биндинг (решено пользователем):** `127.0.0.1:8000` — без внешнего
доступа; проверка запуска — `curl`/SSH-туннель с самого VPS, публичный
доступ не требуется по `urok.md`.

## 3. Шаги

- [x] **Шаг 1. Актуализировать `CLAUDE.md`**
  - Действие: занести в `CLAUDE.md` два пункта — (а) раздел 1, пункт 5:
    заменить «сейчас активного `Plan-*.md` нет» на «активный `Plan-*.md` —
    `Plan-vps.md` (фаза «16:00–17:30 VPS»)»; (б) раздел 7: добавить пункт о
    том, что у ИИ-помощника нет SSH-доступа к реальному VPS — вся работа с
    сервером выполняется пользователем вручную по подготовленному ИИ списку
    команд, «Факт выполнения» в шагах с реальным SSH заполняет пользователь,
    не ИИ (правило проекта, не разовая договорённость только для этого
    плана). Показать точный diff обоих пунктов на согласование до записи.
  - Результат (ожидаемый): `CLAUDE.md` отражает активный план и явную
    границу «ИИ без доступа к VPS», без расхождений с `spec.md`/этим планом.
  - Факт выполнения: обе правки внесены — раздел 1 п.5 указывает на
    `Plan-vps.md` как активный (`Plan-testing.md` — в архиве); раздел 7
    получил новый пункт про отсутствие SSH-доступа у ИИ-помощника.
  - Проверка: обе правки перечитаны в записанном файле, соответствуют
    показанному тексту.
  - Ошибки: нет.

- [x] **Шаг 2. Проверить готовность репозитория к деплою (ИИ, без VPS)**
  - Действие: проверить `requirements.txt` (перечень пакетов совпадает с
    разрешённым стеком `CLAUDE.md`, раздел 3, без лишнего), `.env.example`
    (только плейсхолдеры, все переменные из реального `.env` присутствуют),
    `.gitignore` (исключает `.env`, `data/*.sqlite3*`, `*.log`, `.venv/`).
  - Результат (ожидаемый): все три файла корректны, деплою на VPS ничего не
    мешает технически.
  - Факт выполнения: выполнено до черновика плана — `requirements.txt`
    (`fastapi`, `uvicorn`, `openai`, `python-dotenv==1.2.2`, без
    `playwright`/dev-инструментов), `.env.example` (6 плейсхолдеров, без
    значений), `.gitignore` (`.env`/`data/*.sqlite3*`/`*.log`/`.venv/`
    исключены, `*.log` покроет и будущий `logs/app.log`) — все три в
    порядке.
  - Проверка: прямое чтение всех трёх файлов, сверка с `CLAUDE.md` раздел 3;
    дополнительно — `grep` по всему коду на `os.getenv`/`os.environ`
    (`ai_client.py`, `test_connection.py`) подтвердил, что все 6 реально
    используемых переменных (`YANDEX_API_KEY`, `YANDEX_FOLDER_ID`,
    `YANDEX_MODEL_URI`, `YANDEX_BASE_URL`, `YANDEX_TEMPERATURE`,
    `YANDEX_MAX_TOKENS`) присутствуют в `.env.example`, лишних нет.
  - Ошибки: нет.

- [x] **Шаг 3. Подготовить точный список команд для пользователя**
  - Вход: ответ пользователя на открытый вопрос раздела 2 (порт/биндинг) и
    на вопрос про git-доступ (репозиторий публичный, подтверждено
    `api.github.com/repos/IVAiris/project` → 200 — HTTPS-клон без
    авторизации, deploy-key/токен не нужны).
  - Действие: собрать проверенную последовательность shell-команд для VPS,
    с исправлениями по итогам самопроверки (версия Python, `venv`-пакет,
    явный путь до `pip`, точный PID вместо `pkill` по шаблону):
    1. `python3.13 --version` (если нет — `python3 --version`; если и это
       не 3.13.x, зафиксировать реальную версию как отклонение, не
       подгонять план под факт молча).
    2. `git clone https://github.com/IVAiris/project.git && cd project`
       (публичный репозиторий, без токена/ключа).
    3. `python3.13 -m venv .venv` (если упадёт с `ensurepip is not
       available` — установить пакет `python3.13-venv` через системный
       пакетный менеджер, это подсказка пользователю, не команда ИИ).
    4. `.venv/bin/pip install -r requirements.txt` (явный путь до `pip`
       внутри `venv`, не полагаться на активацию окружения в той же
       shell-сессии).
    5. `mkdir -p data logs`.
    6. `cp .env.example .env`, затем вручную (не через ИИ) заполнить
       реальными значениями.
    7. Запуск: `nohup .venv/bin/uvicorn app:app --host 127.0.0.1 --port
       8000 > logs/app.log 2>&1 & disown` — сразу следующей командой
       `echo $! > app.pid` (точный PID для последующей остановки, не
       `pkill` по шаблону).
    8. Проверка: `curl -s http://127.0.0.1:8000/` (ожидается HTML главной
       страницы) и `tail -n 50 logs/app.log` (без ошибок Python-трейсбека).
    9. Остановка: `kill "$(cat app.pid)"`, затем `curl` на `/` (ожидается
       отказ соединения — подтверждение, что процесс реально остановлен).
    10. Повторный запуск — те же команды пункта 7, затем снова пункт 8
        (это и есть проверка «повторный запуск без ошибок» из `urok.md`).
  - Результат (ожидаемый): пользователь получает готовый к копированию
    список команд, ничего не приходится придумывать самостоятельно на ходу.
  - Факт выполнения: список из 10 пронумерованных команд подготовлен и
    показан пользователю целиком (не разрозненно по репликам); по итогам
    дополнительной самопроверки найдены и включены 6 исправлений (версия
    Python, `venv`-пакет, явный `pip` внутри `venv`, точный PID вместо
    `pkill`, явная remote-остановка с подтверждением через `curl`, точное
    содержание скриншота Шага 7); дополнительно подтверждено, что
    git-репозиторий публичный (`api.github.com/repos/IVAiris/project` →
    200) — SSH-ключ/токен для клонирования не нужны.
  - Проверка: пользователь подтвердил («да»), что список полон и понятен.
  - Ошибки: первая версия списка не покрывала версию Python, `venv`-пакет,
    точный PID и содержание скриншота — найдено по прямому запросу
    пользователя перепроверить, исправлено до передачи финальной версии.
  - Ошибки: забыть `mkdir -p data logs` до первого запуска (упадёт на
    попытке создать лог-файл или БД в несуществующей директории); забыть
    `disown` (процесс упадёт при закрытии SSH-сессии); использовать `kill`
    без сохранённого PID; принять на веру, что процесс остановился, без
    повторного `curl`.

- [x] **Шаг 4. Закрепить точные версии зависимостей в `requirements.txt`**
  - Действие: заменить нефиксированные `fastapi`/`uvicorn`/`openai` на точные
    версии, реально протестированные локально (`fastapi==0.141.1`,
    `uvicorn==0.52.4`, `openai==3.3.1`) — гарантирует, что `pip install` на
    VPS поставит то же самое поведение, что уже проверено во всех
    предыдущих фазах, а не последние доступные на момент деплоя версии.
  - Результат (ожидаемый): `requirements.txt` полностью зафиксирован по
    версиям, деплой воспроизводим.
  - Факт выполнения: `requirements.txt` обновлён — все 4 зависимости теперь
    с точными версиями (`fastapi==0.141.1`, `uvicorn==0.52.4`,
    `openai==3.3.1`, `python-dotenv==1.2.2` без изменений).
  - Проверка: версии в файле совпадают с выводом `.venv/bin/pip show
    fastapi uvicorn openai` (сверено напрямую перед записью).
  - Ошибки: нет.

- [ ] **Шаг 5. Пользователь выполняет деплой на VPS (реальный SSH, вне доступа ИИ)**
  - Вход: список команд Шага 3 согласован, `requirements.txt` закреплён
    версиями (Шаг 4).
  - Действие: пользователь самостоятельно подключается по SSH и выполняет
    весь список — клонирование, окружение, зависимости, `.env` с реальными
    значениями, запуск в фоне.
  - Результат (ожидаемый): приложение запущено на VPS как один процесс,
    отвечает на локальный `curl`.
  - Факт выполнения: **заполняет пользователь** — что реально произошло,
    включая любые расхождения с ожидаемым (ошибки установки, недостающие
    системные пакеты на VPS и т.п.).
  - Проверка: пользователь присылает точный вывод (не пересказ) команд
    проверки — `curl` на `/`, `tail -n 50 logs/app.log` (без реальных
    значений `.env`, если они туда случайно попадут — не присылать).
  - Ошибки: пользователь может по инерции прислать содержимое `.env` или
    полный вывод с секретами — предупредить заранее, до начала SSH-сессии,
    не после.

- [ ] **Шаг 6. Сверка 9 критериев Фазы 6 `urok.md` по присланным фактам (ИИ, по данным пользователя)**
  - Вход: Шаг 5 завершён, пользователь прислал вывод команд/логов.
  - Действие: построчно сверить с `urok.md` (строки 146–160): код доставлен
    через git; `.venv` создано; зависимости из `requirements.txt`
    установлены; `.env` с реальными значениями создан и не в репозитории;
    проект запущен как один runtime; `data/bot.sqlite3` создаётся по
    ожидаемому пути; в логах нет секретов; повторный запуск после остановки
    проходит без ошибок.
  - Результат (ожидаемый): каждый из 8 критериев (9-й — скриншот, отдельный
    Шаг 7) подтверждён конкретным фактом из присланных данных, не
    предположением.
  - Факт выполнения:
  - Проверка: каждый критерий сопровождён точной цитатой/фактом из вывода
    пользователя (не «пользователь сказал, что работает»).
  - Ошибки: принять на веру пересказ вместо факта; не заметить секрет,
    случайно попавший в присланный лог (в этом случае — не копировать его
    дальше в план/чек-лист, попросить пользователя вычистить и прислать
    заново).

- [ ] **Шаг 7. Скриншот `05-vps-runtime-start.png` (пользователь делает, ИИ проверяет)**
  - Действие: пользователь делает скриншот терминала VPS (или SSH-сессии в
    локальном терминале), в кадре одновременно: успешный вывод `curl` на
    `/` (после повторного запуска из Шага 5, не первого), последние строки
    `tail -n 50 logs/app.log` без Python-трейсбека, и `ps aux | grep
    uvicorn` (или `cat app.pid` + `ps -p`), подтверждающие, что процесс
    реально запущен — не только текст «работает» на словах. ИИ-помощник
    проверяет файл на секреты/ключи/лишние персональные данные перед
    фиксацией в `evidence/screenshots/`.
  - Результат (ожидаемый): файл `05-vps-runtime-start.png` в
    `evidence/screenshots/`, без секретов.
  - Факт выполнения:
  - Проверка: визуальная сверка ИИ-помощником перед тем, как считать шаг
    выполненным.
  - Ошибки: риск попадания в кадр `.env`/API-ключа/пути с секретом в
    терминале.

- [ ] **Шаг 8. Обновить чек-лист `project_plan_24h.md`**
  - Действие: отметить все 9 пунктов раздела «16:00–17:30 VPS» `[x]` по
    факту прохождения Шагов 5–7, со ссылкой на конкретный шаг плана.
  - Результат (ожидаемый): весь раздел — `[x]`.
  - Факт выполнения:
  - Проверка: каждый `[x]` сопровождён ссылкой на шаг этого плана и
    конкретным фактом, не намерением.
  - Ошибки: отметить пункт по намерению — уже случавшийся в проекте класс
    ошибки, явно запрещённый `CLAUDE.md`.

## 4. Прямые зависимости и проверки

- `urok.md`, строки 146–160, 200, 219–220 — точные критерии Фазы 6 и формат
  5-го скриншота.
- `spec.md`, разделы 7 и 12 — способ деплоя, границы ответственности
  ИИ/пользователя.
- `server_summery.md` (вне `project/`) — эталонный паттерн `nohup`/логов
  «пятого модуля».
- `requirements.txt`, `.env.example`, `.gitignore` — уже проверены в Шаге 2.
- `CLAUDE.md`, раздел 7 — правило секретов (`.env` не читается/не
  печатается ИИ-помощником).

## 5. Критерии готовности

- Все 9 пунктов раздела «16:00–17:30 VPS» `project_plan_24h.md` — `[x]`,
  каждый со ссылкой на шаг этого плана и присланный пользователем факт.
- `evidence/screenshots/05-vps-runtime-start.png` существует, без секретов.
- Ни один реальный секрет (`.env`, API-ключ) не был напечатан или процитирован
  ИИ-помощником в переписке или в файлах плана.
- Повторный запуск на VPS подтверждён явным фактом, не предположением.
